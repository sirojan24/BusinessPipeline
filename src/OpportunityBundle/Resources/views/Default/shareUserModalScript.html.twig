<script>
    var $sharedUserTable = $('#sharedUserTable');
    
    function showSharedUserModal(users){
        $sharedUserTable.bootstrapTable('removeAll');
        $sharedUserTable.bootstrapTable('append', users);
        $('#sharedUserTable').modal('hide');
    }
</script>